import { SalesLayout } from '@/components/layout/SalesLayout'

export default function SalesRouteLayout({ children }: { children: React.ReactNode }) {
  return <SalesLayout>{children}</SalesLayout>
}
